PORT=3000

# Notification integration
NOTIFICATION_EVENTS_API=http://localhost:3004/api/events
# Required for current book-service integration mode
NOTIFICATION_SERVICE_TOKEN=replace-with-shared-service-token
# Optional target user for in-app admin book notifications
NOTIFICATION_TARGET_USER_ID=system-admin
