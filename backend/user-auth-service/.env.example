PORT=3001
MONGO_URI=mongodb://localhost:27017/book-corner-users
JWT_SECRET=replace-with-user-jwt-secret

# Notification integration
NOTIFICATION_EVENTS_API=http://localhost:3004/api/events
INTERNAL_JWT_SECRET=replace-with-internal-service-secret
# Optional fallback if you use static service token mode instead of JWT
NOTIFICATION_SERVICE_TOKEN=
